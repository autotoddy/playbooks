---
- name: test play
  hosts: localhost
  gather_facts: no
  tasks:
    - name: 
      version_admin_new: '{{version}}'
      when: version_admin == ''

    - name: 
      version_admin_new: '{{version_admin}}'
      when: version_admin != ''

    - debug: var=version_admin_new
